# UI v14 Notes

- Updated Add Item form placeholders to use database-style data type hints.
- Text fields now use `Enter varchar`.
- Quantity now uses `Enter integer`.
- Price now uses `Enter decimal`.
- Special Note now uses `Enter text`.
- Enum fields such as Category and Stock Status no longer use data-type placeholders.
