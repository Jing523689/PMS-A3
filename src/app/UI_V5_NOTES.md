# UI v5 update notes

- Restored Stock Status as a visible editable Ionic `ion-select` field in Add Item.
- Merged all status/featured/note controls into the main Add/Edit form area instead of using a separate status section.
- Added an optional smart status helper that suggests a stock status from quantity without overriding the user's manual choice.
- Updated the app font stack to Manrope with strong system fallbacks for a cleaner mobile UI.
- Added refined typography and status-field styling for clearer attribute names and values.
