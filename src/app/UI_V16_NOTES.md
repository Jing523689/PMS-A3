# UI V16 Search Interaction Update

- Tab 1 search no longer runs while the user types.
- The inventory list changes only after the user taps **Search Item**.
- Segment filters still work on the current list or on the active search result.
- Clearing the search bar resets the inventory view.
- Search remains exact: returned item names must fully match the entered item name.
